// Sidebar layout config
export default {
  // Sidebar default width. Default is 300
  width: 232,

  // Sidebar width when collapsed. Default is 60
  collapsedWidth: 60,

  // Distinct mode
  // Only one menu can be opened at a time
  distinctMenu: true,

  // Icon container size. Default is 36
  iconContainerSize: 36,
}
